<<<<<<< HEAD
# KFS Spring Boot Application Configuration
# Converted from kfs-default-config.properties

spring:
  application:
    name: kfs

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/kfs?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${KFS_DB_USERNAME:kfs}
    password: ${KFS_DB_PASSWORD:kfs}
    hikari:
      maximum-pool-size: 50
      minimum-idle: 5
      connection-timeout: 100000
      idle-timeout: 30000

  jpa:
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  security:
    user:
      name: ${SECURITY_USER_NAME:admin}
      password: ${SECURITY_USER_PASSWORD:changeme}

  quartz:
    job-store-type: memory
    properties:
      org.quartz.threadPool.threadCount: 8
      org.quartz.scheduler.instanceName: KfsQuartzScheduler

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    cache: false

  liquibase:
    enabled: false

server:
  port: 8080
  servlet:
    context-path: /kfs-dev
    session:
      timeout: 90m

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when-authorized

# KFS-specific configuration
kfs:
  version: 6.0.1-SNAPSHOT
  application-id: KFS
  service-namespace-uri: http://kfs.kuali.org/kfs/v5_0
  app-code: kfs
  environment: dev
  production-environment-code: prd
  locale: en_US
  base-url: http://localhost:8080
  base-directory: ${user.home}/kuali/kfs
  module:
    access-security-enabled: false
    labor-distribution-enabled: true
    contracts-and-grants-enabled: true
    external-kuali-coeus-enabled: false
    accounts-receivable-enabled: true
    purchasing-enabled: true
    capital-asset-enabled: true
    travel-enabled: true
  contracts-grants-billing-enabled: false
  directories:
    logs: ${kfs.base-directory}/logs/${kfs.environment}/${kfs.app-code}
    work: ${kfs.base-directory}/work/${kfs.environment}/${kfs.app-code}
    reports: ${kfs.directories.work}/reports
    staging: ${kfs.directories.work}/staging
    temp: ${kfs.directories.work}/temp
    templates: ${kfs.directories.work}/templates
  batch:
    schedule-cron: "00 00 23 ? * *"
    quartz-thread-pool-size: 8
    quartz-trigger-check-interval: 30000
  rice:
    application: kr
    version: 2.1.10

logging:
  level:
    org.kuali: INFO
    org.springframework: INFO
||||||| 65ea4639f2
=======
# KFS Spring Boot Configuration
# Secrets are externalized via environment variables or Spring Cloud Vault.
# No secrets are stored in the repository.

spring:
  application:
    name: kfs
  datasource:
    url: ${KFS_DATASOURCE_URL:jdbc:mysql://localhost:3306/kfs}
    username: ${KFS_DATASOURCE_USERNAME:kfs}
    password: ${KFS_DATASOURCE_PASSWORD:}
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.xml

  cloud:
    vault:
      enabled: ${VAULT_ENABLED:false}
      uri: ${VAULT_URI:http://localhost:8200}
      token: ${VAULT_TOKEN:}
      kv:
        backend: secret
        default-context: kfs

# Actuator endpoints (replaces JavaMelody)
management:
  endpoints:
    web:
      exposure:
        include: health,metrics,info,env,liquibase
  endpoint:
    health:
      show-details: when-authorized
    metrics:
      enabled: true
    info:
      enabled: true
  info:
    build:
      enabled: true
    java:
      enabled: true
    os:
      enabled: true

# KFS-specific properties (previously in kfs-security-skel-config.properties)
kfs:
  datasource:
    url: ${KFS_DATASOURCE_URL:}
    username: ${KFS_DATASOURCE_USERNAME:}
    password: ${KFS_DATASOURCE_PASSWORD:}
  rice:
    datasource:
      url: ${RICE_DATASOURCE_URL:}
      username: ${RICE_DATASOURCE_USERNAME:}
      password: ${RICE_DATASOURCE_PASSWORD:}
  keystore:
    alias: ${KFS_KEYSTORE_ALIAS:}
    password: ${KFS_KEYSTORE_PASSWORD:}
  encryption:
    key: ${KFS_ENCRYPTION_KEY:}
  b2b:
    user-agent: ${B2B_USER_AGENT:}
    purchase-order:
      identity: ${B2B_PURCHASE_ORDER_IDENTITY:}
      password: ${B2B_PURCHASE_ORDER_PASSWORD:}
    shopping:
      identity: ${B2B_SHOPPING_IDENTITY:}
      password: ${B2B_SHOPPING_PASSWORD:}
>>>>>>> origin/devin/1781029206-s5c-hardening
